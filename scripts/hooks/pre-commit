#!/usr/bin/env bash
set -euo pipefail

if command -v bash >/dev/null 2>&1; then
  bash -eu -o pipefail "$(git rev-parse --show-toplevel)"/scripts/hooks/run-pre-commit.sh
else
  echo "bash is not available; cannot run pre-commit pipeline" >&2
  exit 1
fi
